{
  "qformer_hidden_size": 768,
  "blocks_num": 6,
  "num_heads": 12,
  "num_object_queries": 32,
  "num_relation_queries": 48,
  "num_global_queries": 32,
  "num_epochs": 50,
  "batch_size": 48,
  "lr": 1e-4,
  "betas": [0.9, 0.999],
  "dropout_rate": 0.1,
  "weight_decay": 0.01,
  "eps": 1e-8,
  "sequence_size": 32,
  "unfreeze_layers": 2,
  "use_clip_for_text": true,
  "gradient_clip_norm": 1.0,
  "warmup_steps": 500,
  "patience": 15,
  "seed": 42,

  "comments": {
    "batch_size": "48 - slightly smaller due to hierarchical model memory usage",
    "num_relation_queries": "48 instead of 64 - balanced for N^2 pairwise computation",
    "patience": "15 - more patience for complex hierarchical model to converge"
  }
}

